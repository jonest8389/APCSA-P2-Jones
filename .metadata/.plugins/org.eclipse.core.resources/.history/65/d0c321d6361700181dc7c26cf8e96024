//© A+ Computer Science  -  www.apluscompsci.com
//Name -
//Date -
//Class -
//Lab  -

import static java.lang.System.*;

import java.util.Arrays;

public class RomanNumeral
{
	private Integer number = 0;
	private String roman = "";
	private int wasroman = 0;

	private final static int[] NUMBERS= {1000,900,500,400,100,90,
													50,40,10,9,5,4,1};

	private final static String[] LETTERS = {"M","CM","D","CD","C","XC",
												  "L","XL","X","IX","V","IV","I"};

	public RomanNumeral(String str)
	{
		setRoman(str);
	}

	public RomanNumeral(Integer orig)
	{
		setNumber(orig);
	}

	public void setNumber(Integer num)
	{
		roman = "";
		number = num;
		int newnum = number;
		for (int i = 0;i < NUMBERS.length;i++)
		{
			while (newnum - NUMBERS[i] >= 0)
			{
				newnum = newnum - NUMBERS[i];
				roman = roman + LETTERS[i];
			}
		}
		wasroman = 1;
	}
		

	public void setRoman(String rom)
	{
		String dli = "";
		number = 0;
		roman = rom;
		for (int i = 0;i < roman.length();i++)
		{
			char romani = roman.charAt(i);
			char romanis = roman.charAt((i + 1));
			if (i != roman.length() - 1)
			{
				if (NUMBERS[Arrays.asList(LETTERS).indexOf(i)] < 
						NUMBERS[Arrays.asList(LETTERS).indexOf(i + 1)])
				{
					dli = Character.toString(romani)
							+ Character.toString(romanis);
					number = number + 
							NUMBERS[Arrays.asList(LETTERS).indexOf(dli)];
					roman.replace(romani, ' ');
					roman.replace(romanis, ' ');
				}
				else
				{
					number = number + 
							NUMBERS[Arrays.asList(LETTERS).indexOf(i)];
					roman.replace(romani, ' ');
				}
			}
			else
			{
				number = number + 
						NUMBERS[Arrays.asList(LETTERS).indexOf(i)];
				roman.replace(romani, ' ');
			}
		}
		roman.replaceAll(" ", "");	
		wasroman = 2;
	}

	public Integer getNumber()
	{
		return number;
	}

	public String toString()
	{
		if (wasroman == 1)
		{
			return roman + "\n";
		}
		else if (wasroman == 2)
		{
			return number + "\n";
		}
		else
		{
			return null;
		}
	}
}